# Leave empty for local development (Vite proxies /api to localhost:5000).
# In production (Vercel), set this to your deployed backend origin, e.g.:
# VITE_API_URL=https://your-backend.up.railway.app
VITE_API_URL=
